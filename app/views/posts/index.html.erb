<h1>投稿一覧</h1>

<%= link_to "新規作成", "/posts/new" %>
<% @posts.each do |post| %>
  <div>
    <%= link_to post.name , "/posts/#{post.id}" %>
  </div>
  <div>
    <%= post.description %>
  </div>
<% end %>
